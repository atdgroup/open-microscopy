#ifndef __LOCATIONS_CONFIG__
#define __LOCATIONS_CONFIG__

#define PROJECT_NAME "${PROJECT_NAME}"
#define TOPLEVEL_SOURCE_DIRECTORY "${MICROSCOPY_TOPLEVEL_SOURCE_DIR_ESCAPED}"
#define RUNTIME_OUTPUT_DIRECTORY "${MICROSCOPY_RUNTIME_OUTPUT_DIR_ESCAPED}"
#define RESOURCES_DIRECTORY "${MS_MICROSCOPY_DIR_PATH_ESCAPED}\\Resources"

#endif